import React from 'react';
import { Card, CardTitle } from 'material-ui/Card';

const Home = () => (
  <Card className="container">
    <CardTitle title="react application" subtitle="home" />
  </Card>
);

export default Home;
